export default (name) => {

    switch(name) {
        case 'eFresh': {
            return require('../assets/logo.png')
        }
        default: {
            return require('../assets/logo.png')
        }
    }
}